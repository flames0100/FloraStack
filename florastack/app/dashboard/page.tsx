
'use client';
import { useState } from 'react';
export default function Dashboard(){ const [input,setInput]=useState(''); const [messages,setMessages]=useState([]); async function send(){ if(!input) return; setMessages(prev=>[...prev,{role:'user',content:input}]); setMessages(prev=>[...prev,{role:'assistant',content:'Thinking...'}]); setInput(''); } return (<section className="container py-12"><h1 className="text-2xl font-bold">AI Assistant</h1><div className="mt-6 grid md:grid-cols-3 gap-6"><div className="md:col-span-2"><div className="p-4 border rounded-lg card min-h-[300px]">{messages.map((m,i)=>(<div key={i} className={`p-2 my-2 rounded ${m.role==='user'?'bg-flora-50':'bg-slate-50 dark:bg-slate-800'}`}><strong>{m.role==='user'?'You':'FloraAI'}:</strong> {m.content}</div>))}</div><div className="mt-4 flex gap-2"><input value={input} onChange={(e)=>setInput(e.target.value)} className="flex-1 p-3 border rounded-lg" placeholder="Ask Flora..." /><button onClick={send} className="px-4 py-2 bg-flora-500 text-white rounded-lg">Send</button></div></div><aside className="p-4 border rounded-lg card"><h3 className="font-semibold">Insights</h3><div className="mt-3 text-slate-600 dark:text-slate-400">Quick metrics and subscription status will appear here.</div></aside></div></section>); }
